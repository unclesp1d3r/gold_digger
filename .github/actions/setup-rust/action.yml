name: "Setup Rust Environment"
description: "Setup Rust toolchain with common components and caching"

inputs:
  components:
    description: "Rust components to install (comma-separated)"
    required: false
    default: "clippy,rustfmt"
  install-cargo-audit:
    description: "Whether to install cargo-audit"
    required: false
    default: "false"
  install-nextest:
    description: "Whether to install nextest"
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: Install Rust (stable)
      uses: dtolnay/rust-toolchain@stable
      with:
        components: ${{ inputs.components }}

    - name: Rust cache
      uses: Swatinem/rust-cache@v2
      with:
        # Cache key includes OS, architecture, and TLS features for better hit rates and portability
        key: ${{ runner.os }}-${{ runner.arch }}-rust-${{ hashFiles('**/Cargo.lock') }}-${{ matrix.tls || 'default' }}
        # Shared cache across jobs but separate by OS and architecture
        shared-key: ${{ runner.os }}-${{ runner.arch }}-shared
        # Cache build artifacts and registry
        cache-targets: true
        cache-on-failure: true
        # Allow cache writes on main branch and non-PR pushes for better cache utilization
        save-if: ${{ github.ref == 'refs/heads/main' || github.event_name == 'push' }}

    - name: Install nextest
      if: inputs.install-nextest == 'true'
      uses: taiki-e/install-action@v2
      with:
        tool: nextest

    - name: Install cargo-audit
      if: inputs.install-cargo-audit == 'true'
      shell: bash
      run: |
        cargo +stable install cargo-audit --version 0.21.2 --locked
